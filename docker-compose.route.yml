# Route 全部設定由 env/.env.route 提供（勿在此寫死）。
services:
  vbs-route:
    build:
      context: .
      dockerfile: apps/route/Dockerfile
    network_mode: host
    env_file:
      - path: .env.route
        required: false
