# Engine 端 Cloudflare Tunnel（rtc 子網域）
# 僅代理 HTTP/WebSocket 信令；WebRTC 媒體走 ICE/UDP 直連。

tunnel: vbs-engine-rtc
credentials-file: /etc/cloudflared/vbs-engine-rtc.json

ingress:
  - hostname: rtc.yourdomain.com
    service: http://127.0.0.1:5000
  - service: http_status:404
