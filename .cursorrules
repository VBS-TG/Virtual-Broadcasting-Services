# VBS Core Rules (v2.0)

> 任何 AI 助手在本倉庫修改程式碼時，必須遵守本文件。若使用者需求與本文件衝突，先提醒並以本文件為準。

---

## 1) 核心架構：人機分離，身分代理

VBS 採用雙層身份模型：

- **人員層（Human Plane）**：Admin / Guest 的業務登入、租約、權限與審核。
- **機器層（Machine Plane）**：Console 與 Capture/Route/Engine 的節點互信與控制。

兩層解耦，但以 Console Backend 串接：

1. 使用者在前端操作（Human token）。
2. Console 驗證租約/角色與操作權限。
3. Console 以機器憑證代理下發（Machine credential）。

**禁止**將節點機器憑證下放到前端。

---

## 2) 人員驗證（Human Entrance）

- 正式前端入口：`https://vbs.cyblisswisdom.org`
- 人員角色：`admin`、`guest`
- Admin：Email 身分登入 + 白名單（`VBS_ADMIN_EMAILS`）
- Guest：6 碼 PIN / 租賃會話，短效 token，受租期與撤銷控制
- 權限判斷（租約是否有效、可否控制）集中於 Console Backend

### UX 原則（導播化）

- 正式 Console 是「決策中心」，不是終端機。
- 主畫面顯示語意化狀態（可操作、風險、建議），非原始錯誤堆疊。
- 原始錯誤碼/trace 僅放在「可複製診斷資訊」。

---

## 3) 機器驗證（Machine Network）

- Console -> Route/Engine/Capture 一律採 M2M 驗證：
  - `Cf-Access-Client-Id`
  - `Cf-Access-Client-Secret`
- 節點控制面允許：
  - 主路徑：Service Token（自動化）
  - 後備：`Cf-Access-Jwt-Assertion`（人員/維運場景）
- 節點 API 預設不對外開放匿名存取。

---

## 4) 控制面與資料面

- **控制面**：REST/WebSocket（授權、配置、遙測）
- **資料面**：SRT/WebRTC（媒體流）
- 嚴禁在 Console/BFF 轉發媒體二進位資料。
- 媒體傳輸必須維持 SRT AES-256（`VBS_SRT_PASSPHRASE`）。

---

## 5) 前端與 API 邊界（重要）

### 5.1 前端呼叫規範

- 前端優先同源 `/api/*`（BFF 模式）。
- 若採跨域直呼 `vbsapi`，必須確保：
  - Cloudflare Access policy 與 CORS 完整對齊
  - 不得讓日常 API 請求被 302 到 Access login 造成 CORS 中斷

### 5.2 Cloudflare Access 行為

- 人員第一次建立 Access session 可能需互動登入（可跳轉）。
- 機器 M2M 不經互動登入。
- 禁止把「前端 XHR 被 Access 302」當成正常操作流程。

---

## 6) Show Config 與 Orchestrator

Show Config 為單一真相（Single Source of Truth），由 Console 管理：

- 包含：
  - 整線畫質政策（解析度/fps）
  - Source 顯示名與短標籤
  - Switcher 按鈕映射
  - Multiview 映射
- 套用流程必須支援：
  - staged apply
  - 任一步驟失敗可 rollback
  - 下游結果可追蹤

Schema 與契約來源：

- `packages/shared/schemas/show-config.v1.schema.json`
- `protocol.md`

---

## 7) 前端產品分區（導播化）

- **Live（戰術操作）**：Dashboard/Switcher/Telemetry
- **Setup（編排設定）**：Pipeline（Draft 編輯）
- **Show Control（發布控制）**：Show Config 套用/回滾/歷史
- **Health（環境狀態）**：語意化健康檢查

建議安全保護：

- Live Mode 鎖定高風險操作（Apply/Rollback）
- Rollback 採長按或二次確認

---

## 8) 開發與提交規範

- 使用繁體中文撰寫說明/註解（術語可保留英文）
- 不得硬編敏感資訊、token、私鑰、固定公網 IP
- 變更跨模組協定時，必須同步更新：
  - `protocol.md`
  - shared schema / 對應型別
- 提交前至少完成受影響模組 build 或最小 smoke test

---

## 9) 必要環境變數（最小集合）

- `VBS_ADMIN_EMAILS`
- `VBS_GUEST_TOKEN_TTL_SEC`
- `VBS_CONSOLE_JWT_ISSUER`
- `VBS_CONSOLE_JWT_PRIVATE_KEY`
- `VBS_CONSOLE_JWT_PUBLIC_KEYS`
- `VBS_CF_ACCESS_CLIENT_ID`
- `VBS_CF_ACCESS_CLIENT_SECRET`
- `VBS_CF_ACCESS_AUD`
- `VBS_CF_ACCESS_TEAM_DOMAIN` 或 `VBS_CF_ACCESS_JWKS_URL`
- `VBS_CF_JWKS_CACHE_TTL_SEC`
- `VBS_SRT_PASSPHRASE`

以上為強制基準，新增功能不得繞過本規範。